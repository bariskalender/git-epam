# SQL Tasks

This folder contains SQL exercises completed as part of the EPAM trainee program.

- `standalone-scripts` contains numbered scripts for database setup, CRUD operations, joins, views, grouping, and constraints.
- `epam-sql-tasks` contains task-based SQL practice folders with their own README files.

